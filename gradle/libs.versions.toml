[versions]

[libraries]
pebble = { module = "io.pebbletemplates:pebble", version = "3.2.4" }

semver4j = { module = "com.vdurmont:semver4j", version = "3.1.0" }

snakeyaml = { module = "org.yaml:snakeyaml", version = "2.4" }

gradle-docker-plugin = { module = "com.bmuschko:gradle-docker-plugin", version = "9.4.0" }

alice = { module = "to.wetransform:alice", version = "1.0.0" }
